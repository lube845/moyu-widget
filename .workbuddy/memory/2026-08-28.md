# 2026-08-28

- 对 moyu-widget 做了整体健康检查：typecheck 通过、`build:renderer` 构建通过、git 工作区干净。
- 发现的主要问题（未修复，待用户决定）：
  1. `vite.config.js`（编译产物）被提交，且 Vite 优先加载它而非 `vite.config.ts` —— 改 TS 配置不会生效。
  2. `electron/main.js` 恢复窗口位置时不校验显示器范围，换屏后 widget 可能出现在屏幕外找不回。
  3. `WaterTracker` 午夜定时器递归重排后旧 cleanup 清不掉新 timer（跨代泄漏）。
  4. `LunchGacha` 扭蛋滚动的 setTimeout 链卸载不清理；滚动期间可切管理视图删选项，结果可能是已删选项。
  5. Electron 无单实例锁；`.gitignore` 未忽略 `*.tsbuildinfo`。
- 2026 节假日数据核对无误（中秋 9/25-27、国庆 10/1-7、调休 9/20 与 10/10）。

## 修复（同日完成，未提交）

上述问题全部修复完毕：

1. `tsconfig.node.json` 去掉 `composite`、加 `noEmit`，两个 tsconfig 的 tsbuildinfo 重定向到 `node_modules/.tmp/`；`git rm` 了 `vite.config.js`、`vite.config.d.ts`、两个根目录 tsbuildinfo；`.gitignore` 增加 `*.tsbuildinfo`、`vite.config.js`、`vite.config.d.ts`。
2. `electron/main.js`：新增 `isBoundsOnScreen()` 校验恢复位置在任一显示器工作区内；加 `requestSingleInstanceLock()` 单实例锁 + `second-instance` 唤起已有窗口。
3. `WaterTracker.tsx`：午夜定时器 id 存 ref（effect 内局部对象），cleanup 清最新一颗。
4. `LunchGacha.tsx`：`timerRef` + `schedule()` 统一管理滚动/落定定时器，卸载时清理；spinning 期间禁用「✎ 管理」按钮（styles.css 补了 `.manage-toggle:disabled`）。
5. `App.tsx`：pointerdown 时 `setPointerCapture`；`package.json` build 段缩进统一。

验证：`tsc -b --noEmit` 通过、`vite build` 通过（注意：本机 safe-delete 钩子清理旧 dist 偶发失败，手动 `rm -rf dist` 后正常）、`node --check electron/main.js` 通过。改动尚未 commit，等用户决定。

## 当日后续 UI 迭代（同日完成）

- 日历页 highlight 按 `·` 拆两行（后去掉 `·`），CSS 加 `.highlight-line { display:block }`。
- 饮水页：重置按钮从 absolute 改进标题行 flex 流（margin-left:auto），删除"成人建议"副标题；`.water-section { height:100% }` + `.cups-row { margin: auto 0 }` 让杯子组垂直居中；杯子 42px→52px 高；caption 居中。
- 扭蛋页：「✎ 管理」按钮同样移进标题行 flex 流；`.lunch-section { height:100% }` + `.spin-view { flex:1; justify-content:center }`；caption `margin-top:auto` 落底；标题对齐 `.date` 26px/700/-0.5px；数量文字缩到 11px；chip-list max-height 140→180。
- 经验：flex 子项 `margin: auto 0` 可吃掉剩余垂直空间实现居中（注意别写成 `12px auto`，水平 auto 会让宽度收缩）。page-fade 是 absolute 撑满卡片内容区，子 section 用 height:100% 即可拿到固定高度。
- 新增第 5 页「牛马黄历」：`src/lib/niumaAlmanac.ts`（日期种子 hash+mulberry32，宜忌成对词库，每天固定一签）+ `src/components/NiumaCalendar.tsx`；PAGES 加 almanac；样式在 styles.css 末尾 `.almanac-*`。参考 leizongmin/programmer-calendar 的宜忌配对结构，文案为原创牛马梗。


